---
const {image, linkWeb, linkRepo } = Astro.props;
---
<article class="ar-main">
    <img class="ar-main-cover" src={image} alt="porjectos">
    <slot />
    <a href={linkWeb} class="ar-a first-button" rel="noreferrer noopener" target="_blank">Sitio web</a>
    <a href={linkRepo} class="ar-a" rel="noreferrer noopener" target="_blank">Código</a>
</article>

<style>
    .ar-main {
        border: 2px solid light-dark(#333,#fff);
        border-radius: 15px;
        width: 100%;
        height: fit-content;
        overflow: hidden;
    }

    .ar-main-cover {
        height: 200px;
        width: 100%;
        margin: auto;
        object-fit: cover;
    }
    .ar-a {
        display: inline-block;
        background-color: #2FA35F;
        text-decoration: none;
        color: #fff;
        border-radius: 10px;
        font-weight: bold;
        padding: 10px 15px;
        box-sizing: border-box;
        margin-right: 10px;
        margin-bottom: 20px;
    }
    .ar-a:hover {
        background-color: #2fa35fad;
    }
    .first-button {
        margin-left: 20px;
    }
</style>